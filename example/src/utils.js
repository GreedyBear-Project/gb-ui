export const buildGitHubPath = dirName => dirName ? "https://github.com/GreedyBear-Project/gb-ui/tree/main/src/components/" + dirName : "";

export const capitalize = str => str?.length ? str.charAt(0).toUpperCase() + str.slice(1) : "";

